public class Main {

  public static void main() {
    if (diffModel != null && i < diffModel.length && diffModel[i] != null) {
      tailColor = (Color) colors.diff.getDiffColor(colors, diffModel[i].type);
    }
  }

}